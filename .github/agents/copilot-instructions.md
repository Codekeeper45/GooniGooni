# Веб приложение Gooni Gooni Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-04

## Active Technologies
- Python 3.11 (backend), TypeScript + React 18 + Vite 6.3 (frontend) + FastAPI, Pydantic v2, Modal, React Router, Radix UI, motion/react, lucide-react (003-admin-params-gpu-lora)
- SQLite (gallery.db on Modal Volume; local admin.db for password), inference_settings.json, localStorage (presets) (003-admin-params-gpu-lora)

- Python 3.11 (backend), TypeScript 5.x + React 18 (frontend) + FastAPI + Modal (backend), Vite + MUI + Radix (frontend), ComfyUI API (local inference) (002-hybrid-mode-local-remote)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.11 (backend), TypeScript 5.x + React 18 (frontend): Follow standard conventions

## Recent Changes
- 003-admin-params-gpu-lora: Added Python 3.11 (backend), TypeScript + React 18 + Vite 6.3 (frontend) + FastAPI, Pydantic v2, Modal, React Router, Radix UI, motion/react, lucide-react

- 002-hybrid-mode-local-remote: Added Python 3.11 (backend), TypeScript 5.x + React 18 (frontend) + FastAPI + Modal (backend), Vite + MUI + Radix (frontend), ComfyUI API (local inference)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
