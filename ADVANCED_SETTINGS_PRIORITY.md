# Advanced Settings Priority System

## Обзор

Реализована система управления приоритетом между **быстрыми пресетами** и **advanced настройками** для контроля параметров генерации.

## Основные изменения

### 1. Добавлен переключатель "Использовать Advanced"

Новый toggle-переключатель позволяет включать/выключать использование advanced настроек:

- **Выключен (по умолчанию)**: Приоритет у быстрых пресетов
- **Включен**: Приоритет у advanced настроек

### 2. Визуальная индикация

- Переключатель с иконкой `Wand2` и описанием текущего режима
- Баннер предупреждения в секции Advanced Settings, когда они выключены
- Снижение прозрачности (opacity: 0.6) advanced настроек, когда они неактивны

### 3. Логика приоритета

#### Когда `useAdvancedSettings = false`:
- Быстрые пресеты задают параметры (duration, aspect ratio)
- Advanced настройки видны, но помечены как "Просмотр режим"
- При генерации используются значения от быстрых пресетов

#### Когда `useAdvancedSettings = true`:
- Advanced настройки имеют полный приоритет
- Пользователь получает полный контроль над всеми параметрами
- Быстрые пресеты работают, но их значения можно переопределить

### 4. Кнопка "По умолчанию"

Теперь сбрасывает **ТОЛЬКО** advanced параметры:

#### Сбрасываются:
- Negative Prompt → ""
- Seed → -1
- Output Format → "mp4"
- Video parameters (num_frames, fps, motion_score, etc.)
- Image parameters (steps, cfg_scale, sampler, etc.)
- Resolution (width, height) → к дефолтным значениям модели

#### НЕ сбрасываются:
- Prompt (основной промпт пользователя)
- Тип генерации (image/video)
- Выбранная модель
- Режим генерации (t2v, i2v, etc.)

## UI Структура

```
┌─────────────────────────────────────────┐
│  🎨 Использовать Advanced               │
│  [Toggle] Приоритет у быстрых/точных    │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│  ⚙️ Advanced settings  [↕]  [🔄 Reset]  │
└─────────────────────────────────────────┘
```

## Технические детали

### Новые состояния

```typescript
// В MediaGenApp.tsx
const [useAdvancedSettings, setUseAdvancedSettings] = useState(false);
```

### Новые пропсы в ControlPanel

```typescript
interface ControlPanelProps {
  // ...
  useAdvancedSettings: boolean;
  setUseAdvancedSettings: (u: boolean) => void;
  // ...
}
```

### Логика генерации

При генерации учитывается флаг `useAdvancedSettings` для определения финальных значений параметров.

## Примеры использования

### Сценарий 1: Быстрая генерация
1. Пользователь оставляет `useAdvancedSettings = false`
2. Выбирает preset "5с стандарт ★" → автоматически устанавливается num_frames
3. Выбирает "📱 9:16" → автоматически устанавливается 720×1280
4. Генерирует с этими параметрами

### Сценарий 2: Точная настройка
1. Пользователь включает `useAdvancedSettings = true`
2. Открывает Advanced Settings
3. Вручную настраивает num_frames = 97, width = 1024, height = 576
4. Генерирует с точными параметрами

### Сценарий 3: Сброс настроек
1. Пользователь экспериментировал с advanced параметрами
2. Нажимает "По умолчанию"
3. Все advanced параметры сбрасываются к дефолтным значениям модели
4. Prompt остается неизменным

## Файлы изменений

- `/src/app/components/MediaGenApp.tsx` - добавлен state useAdvancedSettings
- `/src/app/components/ControlPanel.tsx` - UI toggle и визуальные индикаторы
- Обновлена функция `handleResetAdvanced()` для сброса только advanced параметров

## Преимущества

✅ Простота использования для новичков (быстрые пресеты)  
✅ Полный контроль для опытных пользователей (advanced)  
✅ Четкая визуальная индикация активного режима  
✅ Кнопка сброса не трогает пользовательский промпт  
✅ Логичное разделение ответственности между UI элементами
