# API endpoint (deployed URL from `modal deploy`)
VITE_API_URL=https://YOUR_WORKSPACE--gooni-api.modal.run

# API key (must match the Modal Secret `gooni-api-key → API_KEY`)
VITE_API_KEY=your-api-key-here

# Admin key for account management UI (must match ADMIN_KEY Modal Secret)
VITE_ADMIN_KEY=your-admin-key-here
