# 📚 Руководство по параметрам Video Generation App

## 🎯 Основные параметры генерации

### **1. Mode (Режим генерации)**
**Тип:** `"t2v" | "i2v"`  
**Расположение:** Верхний переключатель панели управления  
**Значение по умолчанию:** `"t2v"`

- **`t2v` (Text-to-Video)** — генерация видео только на основе текстового описания
- **`i2v` (Image + Text-to-Video)** — генерация видео на основе исходного изображения + текстовой инструкции

**Влияние на процесс:**
- В режиме `t2v` модель создаёт видео "с нуля" из описания
- В режиме `i2v` модель анимирует загруженное изображение согласно текстовой инструкции

---

### **2. Prompt (Основной промпт)**
**Тип:** `string`  
**Макс. длина:** 1000 символов  
**Обязательный:** ✅ Да

**Назначение:**  
Текстовое описание желаемого видео или инструкция по анимации.

**Примеры:**
- **T2V:** `"A lone astronaut walking on Mars, dust storm approaching, cinematic"`
- **I2V:** `"Animate with slow parallax zoom and soft wind movement"`

**Best practices:**
- Описывайте сцену, движение, освещение, стиль камеры
- Используйте кинематографические термины (cinematic, 4K, bokeh, golden hour)
- В режиме I2V описывайте желаемую анимацию, а не саму сцену

**Хранение:** Автоматически сохраняется в `localStorage` под ключом `"vg_prompt"`

---

### **3. Negative Prompt (Негативный промпт)**
**Тип:** `string`  
**Расположение:** Advanced Settings  
**Обязательный:** ❌ Нет

**Назначение:**  
Указывает нежелательные элементы, которые модель должна избегать при генерации.

**Примеры:**
- `"blurry, low quality, distorted, watermark, text"`
- `"camera shake, compression artifacts, pixelated"`

**Влияние на процесс:**  
Модель получает "антиинструкции" и старается не включать эти элементы в результат.

---

## 🎬 Параметры видео

### **4. Duration (Длительность видео)**
**Тип:** `number`  
**Доступные значения:** `4`, `6`, `8` (секунды)  
**Значение по умолчанию:** `4`

**Назначение:**  
Определяет длительность генерируемого видео.

**Влияние на процесс:**
- ⏱️ **Время генерации:** Прямо пропорционально (8s ≈ в 2 раза дольше, чем 4s)
- 🎞️ **Количество кадров:** `duration × 24 fps` (при 4s = 96 кадров, при 8s = 192 кадра)
- 💾 **Размер файла:** Больше длительность = больше файл

**Расчёт времени генерации:**  
`base_time = duration × 6`  
`final_time = base_time × resolution_multiplier`

---

### **5. Resolution (Разрешение)**
**Тип:** `string`  
**Доступные значения:** `"512p"`, `"720p"`  
**Значение по умолчанию:** `"720p"`

**Назначение:**  
Определяет разрешение выходного видео.

**Характеристики:**
- **`512p`** — 512×512 пикселей, быстрая генерация, меньше деталей
- **`720p`** — 720×720 пикселей (или 1280×720), высокое качество, дольше генерация

**Влияние на процесс:**
- ⏱️ **Время генерации:** `720p` на 60% дольше, чем `512p` (`multiplier = 1.6`)
- 💻 **VRAM требования:** Выше разрешение = больше памяти GPU
- 🎨 **Детализация:** Выше разрешение = больше мелких деталей

**Расчёт времени:**
```javascript
estSeconds = duration × 6 × (resolution === "720p" ? 1.6 : 1)
```

---

## 🎨 Параметры стиля и движения

### **6. Motion Intensity (Интенсивность движения)**
**Тип:** `"low" | "medium" | "high"`  
**Значение по умолчанию:** `"medium"`

**Назначение:**  
Контролирует степень динамичности и скорость движения в видео.

**Характеристики:**
- **`low`** — Медленные, плавные движения (subtle motion, slow-mo эффекты)
  - *Примеры:* медленный zoom, легкое колыхание, дрейф камеры
  
- **`medium`** — Сбалансированная динамика (умеренная скорость)
  - *Примеры:* естественные движения, нормальная скорость ходьбы
  
- **`high`** — Быстрые, активные движения (dynamic, action-packed)
  - *Примеры:* быстрые перемещения камеры, активное действие, спорт

**Влияние на процесс:**  
Модель использует этот параметр для калибровки temporal consistency и optical flow между кадрами.

---

### **7. Image Influence (Влияние исходного изображения)**
**Тип:** `number`  
**Диапазон:** `0.0` — `1.0`  
**Шаг:** `0.05`  
**Значение по умолчанию:** `0.6`  
**Только для режима:** `i2v`

**Назначение:**  
Определяет, насколько сильно генерируемое видео должно сохранять визуальные характеристики исходного изображения.

**Влияние на процесс:**
- **`0.1-0.3` (Low)** — Слабое влияние, больше творческой свободы модели
  - Результат может значительно отличаться от оригинала
  - Больше креативности в анимации
  
- **`0.4-0.7` (Medium)** — Сбалансированный подход
  - Сохраняется основная композиция и объекты
  - Достаточно гибкости для интересной анимации
  
- **`0.8-1.0` (Strong)** — Строгое следование исходнику
  - Максимальное сохранение деталей изображения
  - Ограниченная анимация (больше похоже на 2.5D motion)

**Best practices:**
- Для портретов: `0.7-0.9` (сохранить черты лица)
- Для пейзажей: `0.4-0.6` (больше динамики)
- Для motion graphics: `0.3-0.5` (креативная трансформация)

---

## 🔧 Advanced Settings (Продвинутые настройки)

### **8. Seed (Зерно генерации)**
**Тип:** `number | "random"`  
**Диапазон:** `0` — `2,147,483,647` (32-bit integer)  
**Значение по умолчанию:** `"random"`

**Назначение:**  
Инициализирует генератор случайных чисел для воспроизводимости результатов.

**Характеристики:**
- **`"random"`** — Каждый раз новое уникальное видео
- **Конкретное число** — Воспроизводимый результат (при тех же параметрах)

**Использование:**
- 🎲 **Эксперименты:** Оставьте `random` для разнообразия
- 🔁 **Итерации:** Зафиксируйте seed, меняйте другие параметры
- 📋 **Воспроизводимость:** Сохраните seed для точного повторения результата

**Генерация seed:**
```javascript
const randomSeed = Math.floor(Math.random() * 2147483647);
```

**Важно:** Seed НЕ гарантирует 100% идентичный результат при смене модели или версии.

---

### **9. CFG Scale (Classifier-Free Guidance Scale)**
**Тип:** `number`  
**Диапазон:** `1.0` — `20.0`  
**Шаг:** `0.5`  
**Значение по умолчанию:** `7.5`

**Назначение:**  
Контролирует, насколько строго модель следует текстовому промпту.

**Влияние на процесс:**

#### **Низкие значения (1.0 — 4.0) — Creative**
- Модель более свободна в интерпретации
- Больше креативности и вариативности
- Может отклоняться от промпта
- Более естественный, но менее контролируемый результат

#### **Средние значения (5.0 — 10.0) — Balanced** ⭐️ Рекомендуется
- Оптимальный баланс между точностью и качеством
- `7.0-8.0` — стандарт для большинства задач
- Хорошее следование промпту + естественность

#### **Высокие значения (10.0 — 20.0) — Precise**
- Строгое следование каждому слову промпта
- Меньше креативности
- Может привести к артефактам и "перенасыщенности"
- Подходит для технических задач с чёткими требованиями

**Best practices:**
- **Абстрактные концепции:** `5.0-7.0`
- **Конкретные сцены:** `7.0-9.0`
- **Технические требования:** `9.0-12.0`
- **❌ Избегайте:** `>15.0` (риск артефактов)

**Формула влияния:**
```
output = unconditional_output + cfg_scale × (conditional_output - unconditional_output)
```

---

## 📸 Работа с изображениями

### **10. Reference Image (Референсное изображение)**
**Тип:** `File` (PNG, JPG, WebP)  
**Только для режима:** `i2v`  
**Обязательный в I2V:** ✅ Да

**Назначение:**  
Исходное изображение для анимации в режиме Image-to-Video.

**Требования:**
- Форматы: PNG, JPG, JPEG, WebP
- Рекомендуемое разрешение: 512×512 или выше
- Соотношение сторон: желательно квадратное или 16:9

**Методы загрузки:**
1. 📁 **Click to upload** — клик по области загрузки
2. 🖱️ **Drag & drop** — перетаскивание файла
3. 🔄 **Replace** — замена текущего изображения

**Best practices:**
- Высокое качество исходника = лучший результат
- Избегайте слишком сложных композиций для первых экспериментов
- Чёткие, хорошо освещённые изображения работают лучше

---

## 📊 Технические параметры вывода

### **FPS (Frames Per Second)**
**Значение:** `24` (фиксированное)  
**Назначение:** Частота кадров выходного видео

**Расчёт общего количества кадров:**
```javascript
totalFrames = duration × fps
// Примеры:
// 4s → 96 frames
// 6s → 144 frames
// 8s → 192 frames
```

---

### **Model Version**
**Текущая модель:** `"Wan2.1"` (заглушка)

В реальной реализации здесь может быть:
- Версия модели (Wan2.1, CogVideo, Runway Gen-2, etc.)
- Backend endpoint URL
- Специфичные настройки модели

---

## ⏱️ Оценка времени генерации

### **Formula:**
```javascript
function calcEstSeconds(duration: number, resolution: string): number {
  const base = duration × 6;
  const resolutionMultiplier = resolution === "720p" ? 1.6 : 1;
  return Math.round(base × resolutionMultiplier);
}
```

### **Примеры:**
| Duration | Resolution | Est. Time |
|----------|-----------|-----------|
| 4s       | 512p      | ~24s      |
| 4s       | 720p      | ~38s      |
| 6s       | 512p      | ~36s      |
| 6s       | 720p      | ~58s      |
| 8s       | 512p      | ~48s      |
| 8s       | 720p      | ~77s      |

**Факторы влияния на реальное время:**
- 💻 Мощность GPU/CPU
- 🌐 Скорость сети (для облачных API)
- 📊 Загрузка сервера
- 🎨 Сложность промпта
- 🖼️ Размер референсного изображения (для I2V)

---

## 🔄 Состояния генерации

### **GenerationStatus**
**Тип:** `"idle" | "generating" | "done" | "error"`

#### **1. `idle` — Ожидание**
- Начальное состояние
- После сброса ошибки
- Готов к новой генерации

#### **2. `generating` — Процесс генерации**
- UI заблокирован
- Показывается прогресс (0-100%)
- Динамический текст статуса:
  - `0-8%` → "Initializing model..."
  - `8-20%` → "Encoding prompt..."
  - `20-45%` → "Rendering frames..."
  - `45-75%` → "Upscaling..."
  - `75-92%` → "Applying enhancements..."
  - `92-100%` → "Finalizing..."

#### **3. `done` — Успешное завершение**
- Результат отображается в OutputPanel
- Видео добавлено в историю
- Кнопка меняется на "Generate Again"

#### **4. `error` — Ошибка**
- Показывается сообщение об ошибке
- Доступна кнопка "Try Again"
- UI разблокирован для исправления параметров

---

## 📜 История генерации

### **HistoryItem**
**Структура:**
```typescript
interface HistoryItem {
  id: string;              // Уникальный идентификатор (timestamp)
  prompt: string;          // Использованный промпт
  mode: Mode;              // t2v или i2v
  thumbnailUrl: string;    // URL миниатюры результата
  resolution: string;      // Разрешение видео
  duration: number;        // Длительность в секундах
  seed: number;            // Использованный seed
  createdAt: Date;         // Время создания
}
```

**Возможности:**
- 📋 **Reuse parameters** — восстановить все параметры из истории
- 🗑️ **Clear history** — очистить всю историю
- 📊 **Limit:** Хранится до 50 последних результатов

---

## 🎯 Рекомендации по использованию параметров

### **Для быстрых экспериментов:**
```javascript
duration: 4
resolution: "512p"
motionIntensity: "medium"
cfgScale: 7.5
seed: "random"
```

### **Для финального качества:**
```javascript
duration: 6-8
resolution: "720p"
motionIntensity: "medium"
cfgScale: 7.0-8.0
seed: <fixed_number>  // для итераций
```

### **Для креативного поиска:**
```javascript
cfgScale: 5.0-6.0
motionIntensity: "high"
seed: "random"
imageInfluence: 0.3-0.5  // для i2v
```

### **Для точного воспроизведения:**
```javascript
seed: <specific_number>
cfgScale: 8.0-9.0
imageInfluence: 0.7-0.9  // для i2v
```

---

## 🔗 Интеграция с реальным API

### **Payload структура для отправки:**
```typescript
interface GenerationPayload {
  mode: "t2v" | "i2v";
  prompt: string;
  negative_prompt?: string;
  duration: number;
  resolution: string;
  motion_intensity: "low" | "medium" | "high";
  seed: number;
  cfg_scale: number;
  
  // Только для i2v:
  image_base64?: string;
  image_influence?: number;
}
```

### **Ожидаемый response:**
```typescript
interface GenerationResponse {
  videoUrl: string;      // URL сгенерированного видео
  seed: number;          // Использованный seed
  duration: number;      // Длительность
  resolution: string;    // Разрешение
  fps: number;           // FPS
  metadata?: {
    model: string;
    inference_time: number;
    // ... другие метаданные
  };
}
```

---

## 📝 Замечания по хранению данных

### **LocalStorage:**
- ✅ `vg_prompt` — последний промпт (автосохранение)
- ❌ История НЕ сохраняется (теряется при перезагрузке)

### **Для персистентного хранения истории:**
Рассмотрите использование:
- 🗄️ IndexedDB (для локального хранения больших данных)
- ☁️ Supabase / Firebase (для облачного хранения)
- 🔐 Backend API с авторизацией пользователей

---

## 🚀 Performance Tips

1. **Начинайте с низких параметров:**
   - 512p для прототипирования
   - 4s для быстрой итерации

2. **Используйте фиксированный seed:**
   - Для A/B тестирования параметров
   - Для демонстрации влияния отдельных настроек

3. **Оптимизируйте промпты:**
   - Короткие, конкретные описания работают быстрее
   - Избегайте противоречивых инструкций

4. **Мониторьте время генерации:**
   - Используйте `estSeconds` как baseline
   - Корректируйте ожидания в зависимости от железа

---

## 📚 Дополнительные материалы

**Терминология:**
- **CFG (Classifier-Free Guidance)** — метод улучшения контроля над диффузионными моделями
- **Seed** — инициализация PRNG для детерминированности
- **Motion Intensity** — параметр temporal coherence в видеомоделях
- **Image Influence** — вес ControlNet/IP-Adapter в I2V режиме

**Связанные концепции:**
- Temporal consistency (согласованность между кадрами)
- Optical flow (оптический поток движения)
- Latent diffusion (диффузия в латентном пространстве)

---

**Версия документа:** 1.0  
**Дата:** 2026-02-24  
**Приложение:** Video Generation App v2.1
